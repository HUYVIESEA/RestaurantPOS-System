<UserControl x:Class="RestaurantPOS.Desktop.Views.OrderDetailsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             Width="500">
    <materialDesign:Card UniformCornerRadius="10" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="10,10,0,0" Padding="20">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <fa:IconImage Icon="Receipt" Foreground="White" Height="24" Width="24" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Id, StringFormat='Chi tiết đơn hàng #{0}'}" Foreground="White" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                    <Button Style="{StaticResource MaterialDesignIconButton}" HorizontalAlignment="Right" VerticalAlignment="Top"
                            Foreground="White" Margin="0,-10,-10,0"
                            Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                        <fa:IconImage Icon="Times" Foreground="White" Height="16" Width="16"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="20">
                <!-- Info -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <fa:IconImage Icon="Chair" Foreground="Gray" Height="14" Width="14" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding TableId, StringFormat='Bàn: {0}'}" FontSize="14"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="Clock" Foreground="Gray" Height="14" Width="14" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat='dd/MM/yyyy HH:mm'}" FontSize="14"/>
                        </StackPanel>
                    </StackPanel>

                    <Border Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" 
                            CornerRadius="5" Padding="10,4" Background="#E0F2F1">
                        <TextBlock Text="{Binding Status}" Foreground="#00796B" FontWeight="Bold"/>
                    </Border>
                </Grid>

                <Separator Background="#EEE" Margin="0,0,0,10"/>

                <!-- Items List -->
                <TextBlock Text="Danh sách món" FontWeight="Bold" Margin="0,0,0,10"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300">
                    <ItemsControl ItemsSource="{Binding OrderItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding UnitPrice, StringFormat={}{0:N0} đ}" FontSize="12" Foreground="Gray"/>
                                    </StackPanel>
                                    
                                    <TextBlock Grid.Column="1" Text="{Binding Quantity, StringFormat='x{0}'}" Margin="10,0,20,0" VerticalAlignment="Center" FontWeight="Bold"/>
                                    
                                    <TextBlock Grid.Column="2" Text="{Binding TotalPrice, StringFormat={}{0:N0} đ}" VerticalAlignment="Center" FontWeight="Bold"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#FAFAFA" BorderBrush="#EEE" BorderThickness="0,1,0,0" CornerRadius="0,0,10,10" Padding="20">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Tổng cộng:" FontSize="16"/>
                        <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N0} đ}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="10,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,10,0"
                                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                            <StackPanel Orientation="Horizontal">
                                <fa:IconImage Icon="Print" Foreground="{DynamicResource PrimaryHueMidBrush}" Height="16" Width="16" Margin="0,0,5,0"/>
                                <TextBlock Text="In hóa đơn"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="Đóng" 
                                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </materialDesign:Card>
</UserControl>
