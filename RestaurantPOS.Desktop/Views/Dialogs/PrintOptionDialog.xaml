<UserControl x:Class="RestaurantPOS.Desktop.Views.Dialogs.PrintOptionDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Width="350">
    <materialDesign:Card UniformCornerRadius="10" Padding="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <materialDesign:PackIcon Kind="Printer" Foreground="{DynamicResource PrimaryHueMidBrush}" Width="24" Height="24" Margin="0,0,10,0"/>
                <TextBlock Text="Chọn loại phiếu in" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,25" FontSize="14" Foreground="#555"/>

            <StackPanel Orientation="Vertical">
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="Receipt"
                        Content="In Hóa Đơn (Khách)" 
                        Height="40"
                        Margin="0,0,0,10"/>
                
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="Kitchen"
                        Content="In Báo Bếp (Nội bộ)"
                        Height="40"
                         Margin="0,0,0,10"/>

                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="Cancel"
                        Content="Hủy bỏ"/>
            </StackPanel>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
