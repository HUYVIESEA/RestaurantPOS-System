<UserControl x:Class="RestaurantPOS.Desktop.Views.Dialogs.AlertDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Width="300">
    <materialDesign:Card UniformCornerRadius="10" Padding="20">
        <StackPanel>
            <!-- Title -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <materialDesign:PackIcon Kind="InformationCircle" Foreground="{Binding IconColor}" Width="24" Height="24" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message -->
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,25" FontSize="14" Foreground="#555"/>

            <!-- Actions -->
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="True"
                    Content="Đóng" 
                    HorizontalAlignment="Right"
                    Width="80"/>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
