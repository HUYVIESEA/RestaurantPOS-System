<UserControl x:Class="RestaurantPOS.Desktop.Views.CategoriesManagementDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             Width="500" Height="400">
    <materialDesign:DialogHost Identifier="CategoryDialogHost">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Quản lý nhóm hàng" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                <Button Style="{StaticResource MaterialDesignRaisedButton}" HorizontalAlignment="Right"
                        Command="{Binding AddCategoryCommand}" Content="Thêm mới"/>
            </Grid>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Categories}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True"
                      materialDesign:DataGridAssist.CellPadding="13 8 8 8" 
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Id}" Header="ID" Width="60"/>
                    <DataGridTextColumn Binding="{Binding Name}" Header="Tên nhóm" Width="*" FontWeight="Bold"/>
                    <DataGridTextColumn Binding="{Binding Description}" Header="Mô tả" Width="150"/>
                    
                    <DataGridTemplateColumn Header="Thao tác" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Chỉnh sửa"
                                            Command="{Binding DataContext.EditCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Edit" Foreground="Gray" Height="16" Width="16"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Xóa"
                                            Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Trash" Foreground="Red" Height="16" Width="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Style="{StaticResource MaterialDesignFlatButton}" IsCancel="True"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        Content="Đóng"/>
            </StackPanel>
        </Grid>
    </materialDesign:DialogHost>
</UserControl>
