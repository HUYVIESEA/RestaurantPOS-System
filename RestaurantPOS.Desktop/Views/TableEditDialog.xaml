<UserControl x:Class="RestaurantPOS.Desktop.Views.TableEditDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             Width="300">
    <materialDesign:Card UniformCornerRadius="10" Padding="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Center">
                <fa:IconImage Icon="Chair" Foreground="{DynamicResource PrimaryHueMidBrush}" Height="24" Width="24" Margin="0,0,10,0"/>
                <TextBlock Text="Thông tin bàn" FontSize="20" FontWeight="Bold"/>
            </StackPanel>

            <TextBox Text="{Binding TableNumber, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Tên/Số bàn (VD: Bàn 01)"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Margin="0,0,0,15"/>

            <TextBox Text="{Binding Capacity, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Sức chứa (số người)"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Margin="0,0,0,20"/>

            <ComboBox Text="{Binding Floor, UpdateSourceTrigger=PropertyChanged}"
                      materialDesign:HintAssist.Hint="Khu vực / Tầng"
                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      IsEditable="True"
                      Margin="0,0,0,20">
                <sys:String>Tầng 1</sys:String>
                <sys:String>Tầng 2</sys:String>
                <sys:String>Tầng 3</sys:String>
                <sys:String>Tầng 4</sys:String>
                <sys:String>Tầng 5</sys:String>
                <sys:String>Tầng trệt</sys:String>
                <sys:String>Tầng lửng</sys:String>
                <sys:String>Sân thượng</sys:String>
                <sys:String>Khu VIP</sys:String>
                <sys:String>Khu gia đình</sys:String>
                <sys:String>Khu ngoài trời</sys:String>
            </ComboBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Hủy"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                    <Button.CommandParameter>
                        <sys:Boolean>False</sys:Boolean>
                    </Button.CommandParameter>
                </Button>
                <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="Lưu"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                    <Button.CommandParameter>
                        <sys:Boolean>True</sys:Boolean>
                    </Button.CommandParameter>
                </Button>
            </StackPanel>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
