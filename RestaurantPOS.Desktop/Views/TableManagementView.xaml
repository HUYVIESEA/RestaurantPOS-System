<UserControl x:Class="RestaurantPOS.Desktop.Views.TableManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quản lý bàn" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Style="{StaticResource MaterialDesignRaisedButton}" 
                    Command="{Binding AddTableCommand}"
                    Width="150" HorizontalAlignment="Left" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <fa:IconImage Icon="Plus" Foreground="White" Width="16" Height="16" Margin="0,0,10,0"/>
                    <TextBlock Text="Thêm bàn mới"/>
                </StackPanel>
            </Button>
            
            <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                    Command="{Binding RefreshCommand}"
                    ToolTip="Tải lại danh sách">
                <fa:IconImage Icon="Sync" Width="16" Height="16"/>
            </Button>
        </StackPanel>

        <!-- Table List -->
        <materialDesign:Card Grid.Row="2" VerticalAlignment="Stretch">
            <DataGrid ItemsSource="{Binding Tables}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      materialDesign:DataGridAssist.CellPadding="13 8 8 8" 
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Tên bàn" Binding="{Binding TableNumber}" Width="*"/>
                    <DataGridTextColumn Header="Khu vực" Binding="{Binding Floor}" Width="150"/>
                    <DataGridTextColumn Header="Sức chứa" Binding="{Binding Capacity}" Width="100"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding DisplayStatus}" Width="150"/>
                    <DataGridTemplateColumn Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Sửa"
                                            Command="{Binding DataContext.EditTableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Edit" Foreground="{DynamicResource PrimaryHueMidBrush}" Width="16" Height="16"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Xóa"
                                            Command="{Binding DataContext.DeleteTableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Trash" Foreground="#F44336" Width="16" Height="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
        
        <!-- Loading Indicator -->
         <ProgressBar Grid.Row="2" Style="{StaticResource MaterialDesignCircularProgressBar}"
                     Value="0" IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Width="50" Height="50"/>
    </Grid>
</UserControl>
