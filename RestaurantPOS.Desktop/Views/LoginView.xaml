<UserControl x:Class="RestaurantPOS.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    
    <Grid>
        <!-- Gradient Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#667eea" Offset="0"/>
                <GradientStop Color="#764ba2" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Decorative Circles -->
        <Canvas>
            <Ellipse Width="400" Height="400" Fill="#40FFFFFF" Canvas.Left="-100" Canvas.Top="-100"/>
            <Ellipse Width="300" Height="300" Fill="#20FFFFFF" Canvas.Right="-50" Canvas.Bottom="-50"/>
            <Ellipse Width="200" Height="200" Fill="#30FFFFFF" Canvas.Right="100" Canvas.Top="50"/>
        </Canvas>

        <!-- Login Card -->
        <Border Width="450" Height="550" 
                Background="White" 
                CornerRadius="20"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="30" ShadowDepth="10"/>
            </Border.Effect>

            <Grid Margin="50,40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Logo & Title -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <Border Width="80" Height="80" 
                            Background="#667eea" 
                            CornerRadius="40"
                            Margin="0,0,0,15">
                        <TextBlock Text="🍽️" 
                                   FontSize="40" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="RESTAURANT POS" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Foreground="#2D3748"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Đăng nhập hệ thống" 
                               FontSize="14" 
                               Foreground="#718096"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Username Field -->
                <StackPanel Grid.Row="1" Margin="0,0,0,20">
                    <TextBlock Text="Tên đăng nhập" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#4A5568"
                               Margin="0,0,0,8"/>
                    <Border BorderBrush="#E2E8F0" 
                            BorderThickness="2" 
                            CornerRadius="8"
                            Background="#F7FAFC">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="👤" 
                                       FontSize="18" 
                                       Margin="15,0,10,0"
                                       VerticalAlignment="Center"
                                       Foreground="#A0AEC0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                     FontSize="14"
                                     Padding="5,12"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VerticalAlignment="Center">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <Trigger Property="IsFocused" Value="True">
                                                <Setter Property="Background" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Password Field -->
                <StackPanel Grid.Row="2" Margin="0,0,0,25">
                    <TextBlock Text="Mật khẩu" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#4A5568"
                               Margin="0,0,0,8"/>
                    <Border BorderBrush="#E2E8F0" 
                            BorderThickness="2" 
                            CornerRadius="8"
                            Background="#F7FAFC">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔒" 
                                       FontSize="18" 
                                       Margin="15,0,10,0"
                                       VerticalAlignment="Center"
                                       Foreground="#A0AEC0"/>
                            <PasswordBox Grid.Column="1"
                                         x:Name="PasswordBox"
                                         FontSize="14"
                                         Padding="5,12"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Error Message -->
                <Border Grid.Row="3" 
                        Background="#FEE2E2" 
                        BorderBrush="#F87171"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12,10"
                        Margin="0,0,0,20"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ErrorMessage}" 
                                   Foreground="#DC2626"
                                   FontSize="13"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Login Button -->
                <Button Grid.Row="4"
                        Command="{Binding LoginCommand}"
                        Height="50"
                        Margin="0,0,0,15"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#667eea" Offset="0"/>
                                        <GradientStop Color="#764ba2" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                Padding="20,0">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Opacity" Value="0.7"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding IsLoading, Converter={StaticResource LoginButtonTextConverter}}" 
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="⏳" 
                                   FontSize="16" 
                                   Margin="10,0,0,0"
                                   Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </Button>

                <!-- Footer -->
                <TextBlock Grid.Row="6"
                           Text="© 2025 Restaurant POS System"
                           FontSize="12"
                           Foreground="#A0AEC0"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Bottom"
                           Margin="0,20,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
