<Window x:Class="RestaurantPOS.Desktop.Views.ReceiptPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Xem trước hóa đơn" Height="700" Width="450"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Document Viewer for Preview -->
        <FlowDocumentScrollViewer Name="DocViewer" Grid.Row="0" Margin="10" 
                                  VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollBarVisibility="Disabled"
                                  BorderThickness="1" BorderBrush="#EEE">
        </FlowDocumentScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="1" Padding="15" Background="{DynamicResource MaterialDesignCardBackground}" Effect="{StaticResource MaterialDesignShadowDepth1}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Hủy bỏ" Click="Cancel_Click" Margin="0,0,10,0"/>
                <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="In Hóa Đơn" Click="Print_Click">
                    <Button.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding PrintCommand}"/>
                    </Button.InputBindings>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
