<UserControl x:Class="RestaurantPOS.Desktop.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantPOS.Desktop.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Filter Bar -->
        <materialDesign:Card Grid.Row="0" Margin="20" Padding="20" UniformCornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Danh sách hàng hóa" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    
                    <!-- Category Filter -->
                    <ComboBox materialDesign:HintAssist.Hint="Lọc theo nhóm" Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                              ItemsSource="{Binding Categories}" DisplayMemberPath="Name"
                              SelectedItem="{Binding SelectedCategory}"
                              Width="150" Margin="0,0,20,0" VerticalAlignment="Bottom"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,10,0" Content="Nhập Excel" Command="{Binding ImportExcelCommand}"/>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,10,0" Content="Nhóm hàng" Command="{Binding ManageCategoriesCommand}"/>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="Thêm mới" Command="{Binding AddProductCommand}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Products DataGrid -->
        <materialDesign:Card Grid.Row="1" Margin="20,0,20,20" UniformCornerRadius="5" VerticalAlignment="Stretch">
            <DataGrid ItemsSource="{Binding ProductsView}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True"
                      SelectionMode="Single" SelectionUnit="FullRow"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      materialDesign:DataGridAssist.CellPadding="13 8 8 8" 
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <!-- Image -->
                    <DataGridTemplateColumn Header="Hình ảnh" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="40" Height="40" CornerRadius="5" Background="#EEE" ClipToBounds="True">
                                    <Image Source="{Binding ImageUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Binding="{Binding Id}" Header="Mã SP" Width="80"/>
                    <DataGridTextColumn Binding="{Binding Name}" Header="Tên hàng hóa" Width="*" FontWeight="Bold"/>
                    <DataGridTextColumn Binding="{Binding CategoryName}" Header="Nhóm hàng" Width="150"/>
                    
                    <DataGridTemplateColumn Header="Giá bán" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Price, StringFormat={}{0:N0} đ}" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Binding="{Binding Unit}" Header="ĐVT" Width="80"/>

                    <DataGridTemplateColumn Header="Thao tác" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Chỉnh sửa"
                                            Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Edit" Foreground="Gray" Height="16" Width="16"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Xóa"
                                            Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Trash" Foreground="Red" Height="16" Width="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <!-- Pagination Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding PreviousPageCommand}" Content="Trước"/>
            <TextBlock Text="{Binding CurrentPage, StringFormat='Trang {0}'}" VerticalAlignment="Center" Margin="10,0"/>
            <TextBlock Text="/" VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" Margin="10,0"/>
            <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding NextPageCommand}" Content="Sau"/>
        </StackPanel>
    </Grid>
</UserControl>
