<UserControl x:Class="RestaurantPOS.Desktop.Views.UserDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             Width="400">
    <materialDesign:Card UniformCornerRadius="8" Padding="20">
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Identity -->
            <TextBox materialDesign:HintAssist.Hint="Tên đăng nhập" Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding Username}" IsReadOnly="{Binding IsEditMode}" Margin="0,0,0,10"/>
            
            <PasswordBox x:Name="PasswordBox" materialDesign:HintAssist.Hint="Mật khẩu" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,0,0,10" Visibility="{Binding IsEditMode, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

            <TextBox materialDesign:HintAssist.Hint="Họ và tên" Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding FullName}" Margin="0,0,0,10"/>

            <TextBox materialDesign:HintAssist.Hint="Email" Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding Email}" Margin="0,0,0,10"/>

            <TextBox materialDesign:HintAssist.Hint="Số điện thoại" Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding Phone}" Margin="0,0,0,10"/>

            <ComboBox materialDesign:HintAssist.Hint="Vai trò" Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      Text="{Binding Role}" Margin="0,0,0,10">
                <ComboBoxItem Content="Admin"/>
                <ComboBoxItem Content="Manager"/>
                <ComboBoxItem Content="Staff"/>
            </ComboBox>

            <CheckBox Content="Đang hoạt động" IsChecked="{Binding IsActive}" Margin="0,10,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Hủy" 
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        Margin="0,0,10,0"/>
                
                <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="Lưu" 
                        Click="SaveButton_Click"/>
            </StackPanel>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
