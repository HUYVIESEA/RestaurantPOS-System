<Window x:Class="RestaurantPOS.Desktop.Controls.ToastNotification"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Toast" 
        Height="80" 
        Width="400"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual">
    
    <Border x:Name="ToastBorder" 
            Background="#323232" 
            CornerRadius="8" 
            Padding="20,15"
            Opacity="0">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="15" ShadowDepth="3"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <TextBlock x:Name="IconText" 
                       Grid.Column="0" 
                       FontSize="24" 
                       VerticalAlignment="Center" 
                       Margin="0,0,15,0"
                       Foreground="White"/>

            <!-- Message -->
            <TextBlock x:Name="MessageText" 
                       Grid.Column="1" 
                       Text="{Binding Message, RelativeSource={RelativeSource AncestorType=Window}}" 
                       TextWrapping="Wrap" 
                       VerticalAlignment="Center" 
                       FontSize="14"
                       Foreground="White"
                       FontWeight="Medium"/>
        </Grid>
    </Border>
</Window>
