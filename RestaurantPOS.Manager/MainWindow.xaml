<Window x:Class="RestaurantPOS.Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestaurantPOS.Manager"
        mc:Ignorable="d"
        Title="Restaurant POS Manager" Height="720" Width="1080"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}"
        FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="200"/>  <!-- Logs -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{StaticResource PrimaryColor}" CornerRadius="8" Width="40" Height="40" Margin="0,0,15,0">
                    <TextBlock Text="M" Foreground="White" FontWeight="Bold" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel>
                    <TextBlock Text="Restaurant POS Manager" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextColor}"/>
                    <TextBlock Text="System Control Center" FontSize="14" Foreground="{StaticResource SubTextColor}"/>
                </StackPanel>
            </StackPanel>
            <TextBlock x:Name="ActiveProcessCount" Text="Active Processes: 0" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource SubTextColor}"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column: Status & Monitor -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <!-- System Status Card -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="System Status" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource TextColor}"/>
                        
                        <!-- API Status -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="ApiStatusIndicator" Style="{StaticResource StatusDotStyle}" Fill="#EF4444"/>
                                <TextBlock Text="Backend API (Port 5000)" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock x:Name="ApiStatusText" Grid.Column="1" Text="Stopped" Foreground="#EF4444" FontWeight="SemiBold"/>
                        </Grid>

                        <!-- Web Client Status -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="WebStatusIndicator" Style="{StaticResource StatusDotStyle}" Fill="#EF4444"/>
                                <TextBlock Text="Web Client (Port 5173)" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock x:Name="WebStatusText" Grid.Column="1" Text="Stopped" Foreground="#EF4444" FontWeight="SemiBold"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Resource Monitor Card -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Resource Monitor" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource TextColor}"/>
                        
                        <!-- CPU -->
                        <TextBlock Text="CPU Usage" Margin="0,0,0,5" Foreground="{StaticResource SubTextColor}"/>
                        <Grid Margin="0,0,0,15">
                            <ProgressBar x:Name="CpuProgressBar" Height="10" Background="#E5E7EB" BorderThickness="0" Foreground="{StaticResource PrimaryColor}"/>
                            <TextBlock x:Name="CpuUsageText" Text="0%" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-20,0,0" FontSize="12"/>
                        </Grid>

                        <!-- RAM -->
                        <TextBlock Text="RAM Usage" Margin="0,0,0,5" Foreground="{StaticResource SubTextColor}"/>
                        <Grid>
                            <ProgressBar x:Name="RamProgressBar" Height="10" Background="#E5E7EB" BorderThickness="0" Foreground="{StaticResource WarningColor}"/>
                            <TextBlock x:Name="RamUsageText" Text="0%" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-20,0,0" FontSize="12"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right Column: Actions -->
            <StackPanel Grid.Column="1">
                <!-- Services Control Card -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Service Controls" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource TextColor}"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- API Controls -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Backend API" FontWeight="SemiBold" Margin="5,0,0,5"/>
                                <Button Content="Start API" Click="BtnStartApi_Click" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource SuccessColor}"/>
                                <Button Content="Stop API" Click="BtnStopApi_Click" Style="{StaticResource OutlineButtonStyle}"/>
                            </StackPanel>

                            <!-- Web Controls -->
                            <StackPanel Grid.Column="1" Margin="5,0,5,0">
                                <TextBlock Text="Web Client" FontWeight="SemiBold" Margin="5,0,0,5"/>
                                <Button Content="Start Client" Click="BtnStartWeb_Click" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource SuccessColor}"/>
                                <Button Content="Stop Client" Click="BtnStopWeb_Click" Style="{StaticResource OutlineButtonStyle}"/>
                            </StackPanel>

                            <!-- Desktop Controls -->
                            <StackPanel Grid.Column="2" Margin="10,0,0,0">
                                <TextBlock Text="Desktop App" FontWeight="SemiBold" Margin="5,0,0,5"/>
                                <Button Content="Launch App" Click="BtnStartDesktop_Click" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource PrimaryColor}"/>
                                <Button Content="Stop All" Click="BtnStopAll_Click" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource DangerColor}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Database & Tools Card -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Database &amp; Tools" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{StaticResource TextColor}"/>
                        
                        <WrapPanel>
                            <Button Content="Update DB" Click="BtnUpdateDb_Click" Style="{StaticResource OutlineButtonStyle}" Width="110"/>
                            <Button Content="Seed Data" Click="BtnSeedDb_Click" Style="{StaticResource OutlineButtonStyle}" Width="110"/>
                            <Button Content="Install Deps" Click="BtnInstallDeps_Click" Style="{StaticResource OutlineButtonStyle}" Width="110"/>
                            <Button Content="Build Prod" Click="BtnBuildProd_Click" Style="{StaticResource OutlineButtonStyle}" Width="110"/>
                            <Button Content="Edit Config" Click="BtnEditConfig_Click" Style="{StaticResource OutlineButtonStyle}" Width="110"/>
                            <Button Content="Clean Project" Click="BtnClean_Click" Style="{StaticResource OutlineButtonStyle}" Width="110"/>
                            <Button Content="Setup Firewall" Click="BtnFirewall_Click" Style="{StaticResource OutlineButtonStyle}" Width="120"/>
                            <Button Content="Swagger UI" Click="BtnSwagger_Click" Style="{StaticResource OutlineButtonStyle}" Width="110"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Logs Section -->
        <Border Grid.Row="2" Background="#111827" CornerRadius="12" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="System Logs" Foreground="#9CA3AF" FontSize="12" Margin="0,0,0,10"/>
                <ScrollViewer Grid.Row="1" x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtLogs" Foreground="#10B981" FontFamily="Consolas" FontSize="13" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
